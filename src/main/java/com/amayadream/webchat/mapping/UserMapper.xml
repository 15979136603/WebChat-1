<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.amayadream.webchat.dao.IUserDao">

  <resultMap id="BaseResultMap" type="com.amayadream.webchat.pojo.User">
    <id column="USERID" property="userid" jdbcType="VARCHAR"/>
    <result column="PASSWORD" property="password" jdbcType="VARCHAR"/>
    <result column="NICKNAME" property="nickname" jdbcType="VARCHAR"/>
    <result column="SEX" property="sex" jdbcType="INTEGER"/>
    <result column="AGE" property="age" jdbcType="INTEGER"/>
    <result column="PROFILEHEAD" property="profilehead" jdbcType="VARCHAR"/>
    <result column="PROFILE" property="profile" jdbcType="VARCHAR"/>
    <result column="FIRSTTIME" property="firsttime" jdbcType="VARCHAR"/>
    <result column="LASTTIME" property="lasttime" jdbcType="VARCHAR"/>
    <result column="STATUS" property="status" jdbcType="INTEGER"/>
    <result column="EMAIL" property="email" jdbcType="VARCHAR"/>
    <result column="IMPRESSION1" property="impression1" jdbcType="VARCHAR"/>
    <result column="IMPRESSION2" property="impression2" jdbcType="VARCHAR"/>
    <result column="IMPRESSION3" property="impression3" jdbcType="VARCHAR"/>
    <result column="IMPRESSION4" property="impression4" jdbcType="VARCHAR"/>
    <result column="IMPRESSION5" property="impression5" jdbcType="VARCHAR"/>
    <result column="IMPRESSION6" property="impression6" jdbcType="VARCHAR"/>
  </resultMap>

  <sql id="Base_Column_List">
    USERID, PASSWORD, NICKNAME, SEX, AGE, PROFILEHEAD, PROFILE, FIRSTTIME, LASTTIME, STATUS, EMAIL,IMPRESSION1,IMPRESSION2,IMPRESSION3,
    IMPRESSION4,IMPRESSION5,IMPRESSION6
  </sql>

  <!-- 查询所有用户 -->
  <select id="selectAll" resultMap="BaseResultMap">
      SELECT  USERID, PASSWORD, NICKNAME, SEX, AGE, PROFILEHEAD, PROFILE, FIRSTTIME, LASTTIME, STATUS, EMAIL FROM user
      ORDER BY FIRSTTIME,IMPRESSION1,IMPRESSION2,IMPRESSION3,
    IMPRESSION4,IMPRESSION5,IMPRESSION6
      LIMIT #{offset}, #{limit}
  </select>

  <!-- 根据用户id查询所有好友 -->
  <select id="selectAllFriend" resultType="String" parameterType="string">
      SELECT  u.USERID FROM user u,friendlist f
      WHERE u.userid = f.friendid AND f.userid = #{userid}
      order by firsttime
  </select>

  <!-- 根据用户id查询用户 -->
  <select id="selectUserByUserid" resultMap="BaseResultMap" parameterType="string">
    SELECT <include refid="Base_Column_List"/>
    FROM user
    WHERE USERID = #{userid, jdbcType=VARCHAR}
  </select>

  <!-- 根据用户id模糊查询用户 -->
  <select id="selectUsers" resultType="com.amayadream.webchat.pojo.User" parameterType="string">
    SELECT *
    FROM user
    WHERE USERID like "%"#{userid}"%"
  </select>

  <!-- 根据用户id查询用户数量 -->
  <select id="selectCount" resultMap="BaseResultMap" >
    SELECT COUNT(*) USERID
    FROM user
  </select>

  <!-- 插入用户 -->
  <insert id="insert" parameterType="com.amayadream.webchat.pojo.User">
    INSERT INTO user (USERID, PASSWORD, NICKNAME, SEX, AGE, PROFILEHEAD, PROFILE, FIRSTTIME, LASTTIME, STATUS, EMAIL,IMPRESSION1,IMPRESSION2,IMPRESSION3,
    IMPRESSION4,IMPRESSION5,IMPRESSION6)
    VALUES (#{userid, jdbcType=VARCHAR}, #{password, jdbcType=VARCHAR}, #{nickname, jdbcType=VARCHAR}, #{sex, jdbcType=INTEGER},
    #{age, jdbcType=INTEGER}, #{profilehead, jdbcType=VARCHAR}, #{profile, jdbcType=VARCHAR},
    #{firsttime, jdbcType=VARCHAR}, #{lasttime, jdbcType=VARCHAR}, #{status, jdbcType=INTEGER},
     #{impression1,jdbcType=VARCHAR},#{impression2,jdbcType=VARCHAR},#{impression3,jdbcType=VARCHAR},
    #{impression4,jdbcType=VARCHAR},#{impression5,jdbcType=VARCHAR},#{impression6,jdbcType=VARCHAR}
    , #{email, jdbcType=VARCHAR})
  </insert>

  <!-- 插入朋友关系 -->
  <insert id="insertFriend" parameterType="String">
    INSERT INTO friendlist (userid,friendid)
    VALUES (#{0, jdbcType=VARCHAR}, #{1, jdbcType=VARCHAR})
  </insert>

  <!-- 更新用户 -->
  <update id="update" parameterType="com.amayadream.webchat.pojo.User">
    UPDATE user
    <set>
      <if test="password != null and password != ''">
        PASSWORD = #{password, jdbcType=VARCHAR},
      </if>
      <if test="nickname != null and nickname != ''">
        NICKNAME = #{nickname, jdbcType=VARCHAR},
      </if>
      <if test="sex != null and sex != ''">
        SEX = #{sex, jdbcType=INTEGER},
      </if>
      <if test="age != null and age != ''">
        AGE = #{age, jdbcType=INTEGER},
      </if>
      <if test="profilehead != null and profilehead != ''">
        PROFILEHEAD = #{profilehead, jdbcType=VARCHAR},
      </if>
      <if test="profile != null and profile != ''">
        PROFILE = #{profile, jdbcType=VARCHAR},
      </if>
      <if test="firsttime != null and firsttime != ''">
        FIRSTTIME = #{firsttime, jdbcType=VARCHAR},
      </if>
      <if test="lasttime != null and lasttime != ''">
        LASTTIME = #{lasttime, jdbcType=VARCHAR},
      </if>
      <if test="status != null and status != ''">
        STATUS = #{status, jdbcType=INTEGER},
      </if>
      <if test="email != null and email != ''">
        EMAIL = #{email, jdbcType=VARCHAR},
      </if>
      <if test="impression1!=null and impression1 !=''">
        IMPRESSION1=#{impression1,jdbcType=VARCHAR},
      </if>
      <if test="impression2!=null and impression2 !=''">
        IMPRESSION2=#{impression2,jdbcType=VARCHAR},
      </if>
      <if test="impression3!=null and impression3 !=''">
        IMPRESSION3=#{impression3,jdbcType=VARCHAR},
      </if>
      <if test="impression4!=null and impression4 !=''">
        IMPRESSION4=#{impression4,jdbcType=VARCHAR},
      </if>
      <if test="impression5!=null and impression5 !=''">
        IMPRESSION5=#{impression5,jdbcType=VARCHAR},
      </if>
      <if test="impression6!=null and impression6 !=''">
        IMPRESSION6=#{impression6,jdbcType=VARCHAR},
      </if>
    </set>
    WHERE USERID = #{userid, jdbcType=VARCHAR}
  </update>

  <!-- 删除用户 -->
  <delete id="delete" parameterType="string">
    DELETE FROM user
    WHERE USERID = #{userid, jdbcType=VARCHAR}
  </delete>

  <!-- 删除朋友关系 -->
  <delete id="deleteFriend" parameterType="String">
    DELETE FROM friendlist
    WHERE userid = #{0, jdbcType=VARCHAR} AND friendid = #{1, jdbcType=VARCHAR}
  </delete>

</mapper>